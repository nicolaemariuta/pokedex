/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.imagin.studio', 'img.pokemondb.net']   //here I might have to add the pokemons image url

    }
};

export default nextConfig;
